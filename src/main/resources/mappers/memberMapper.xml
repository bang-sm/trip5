<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.memberMapper">

	<insert id="signup" parameterType="com.sm.domain.MemberVO">
		INSERT INTO member
		(memberid,
		memberpass, memberemail, membernick, memberregdate)
		VALUES
		(#{memberid},#{memberpass},#{memberemail},#{membernick},now());
	</insert>

	<select id="findById" resultType="com.sm.domain.MemberVO"
		parameterType="String">
		SELECT
		*
		FROM
		member
		WHERE
		memberid=#{memberid}
	</select>

	<!-- 아이디 확인 -->
	<select id="idCheck" resultType="com.sm.domain.MemberVO">
		select memberid from member
		where memberid = #{memberid}
	</select>

</mapper>