<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.noticeMapper">

<!-- 	<insert id="signup" parameterType="com.sm.domain.MemberVO">
		INSERT INTO member
			(memberpass,
			 memberemail,
			 membernick,
			 memberregdate)
		VALUES
			(#{memberpass},
			#{memberemail},
			#{membernick},
			now());
	</insert>
 -->
	<!-- 슬라이드 공지 내용 -->
	<select id="slideContent" resultType="String">
		SELECT
			snContent
		FROM
			slideNotice
	</select>
	
		<!-- 슬라이드 공지 개수 -->
	<select id="slideCount" resultType="int">
		select
			count(*)
		from
			slideNotice
	</select>	
</mapper>